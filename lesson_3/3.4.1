# Write two different ways to remove all of the elements from the following
# list:

numbers = [1, 2, 3, 4]

while numbers:
    numbers.pop()
    
print(numbers)

numbers = [1, 2, 3, 4]
print(numbers)

numbers.clear()
print(numbers)