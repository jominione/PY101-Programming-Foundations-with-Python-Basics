# Write two distinct ways of reversing the list wtihtout mutating the
# original list.

numbers = [1, 2, 3, 4, 5]

reverse_numbers1 = numbers[::-1]
reverse_numbers2 = list(reversed(numbers))
print(reverse_numbers1)
print(reverse_numbers2)