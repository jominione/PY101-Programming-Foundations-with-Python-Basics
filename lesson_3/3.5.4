# What will the following two lines of code output?

print(0.3 + 0.6)        # 0.9
print(0.3 + 0.6 == 0.9) # True

# The first print will output a value while the second does a comparison
# and evaluate output to a boolean.